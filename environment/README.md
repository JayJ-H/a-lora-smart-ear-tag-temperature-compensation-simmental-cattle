# Software environment

- `requirements-quick.txt`: dependencies for `run_all_checks.py`;
- `requirements-full.txt`: complete Python analysis dependencies;
- `install_r_packages.R`: R dependencies;
- import inventories: packages detected in source files.

Python 3.12 or newer and R 4.x are supported. Virtual environments and package caches are not included.
