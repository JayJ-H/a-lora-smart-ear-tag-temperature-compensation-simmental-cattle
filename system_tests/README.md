# System checks

```bash
python system_tests/run_system_tests.py
```

Checks cover packet vectors, protocol roundtrips, radio-parameter alignment,
firmware constants, excluded file types, asset hashes, the complete platform
source, MySQL bootstrap files, Node.js syntax, runtime timezone handling,
package-script paths, and deployment templates.
