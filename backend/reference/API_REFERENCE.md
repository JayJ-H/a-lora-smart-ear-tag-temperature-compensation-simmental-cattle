# API reference

The reference interface includes health/status checks, temperature queries, animal/tag mapping, alert review, and gateway-control requests. MQTT ingestion resolves the active tag assignment before storing temperature and link records. Credentials, hosts, databases, and field logs are not included.
