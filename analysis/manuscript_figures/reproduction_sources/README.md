# Figure reproduction materials

Each `figXX/` directory contains the inputs, component assets, scripts, and generated panels for that manuscript figure. Data-driven scripts use repository-relative paths so they can be executed after cloning the repository.
